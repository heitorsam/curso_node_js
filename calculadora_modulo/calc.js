var calcSoma = require("./somar");
var calcDiv = require("./dividir");
var calcMulti = require("./multiplicar");
var calcSub = require("./subtrair");

console.log(calcSoma(1,2));
console.log(calcDiv(10,5));
console.log(calcMulti(2,30));
console.log(calcSub(10,2));